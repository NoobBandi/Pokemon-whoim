# Pokemon Pikachu-ifier - Requirements
#
# Install steps (CUDA / NVIDIA GPU):
#   1. PyTorch with CUDA 11.8:
#      pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu118
#   2. Everything else (from PyPI):
#      pip install -r requirements.txt
#
# Install steps (Apple Silicon / MPS):
#   pip install -r requirements.txt
#
# After install, verify with: pip check

# Deep Learning (install separately with --index-url for CUDA)
torch==2.7.1
torchvision==0.22.1

# Stable Diffusion + ControlNet + IP-Adapter
diffusers==0.31.0
transformers==4.47.0
accelerate==1.2.1
safetensors==0.4.5
sentencepiece==0.2.0

# Image Processing
Pillow==11.1.0
opencv-python-headless==4.11.0.86

# Utilities
tqdm==4.67.1
matplotlib==3.10.0

# Dataset Download (optional)
kaggle==1.7.4.2
